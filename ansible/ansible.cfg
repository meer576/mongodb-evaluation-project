[defaults]
inventory = inventory/aws_ec2.yml
host_key_checking = False
remote_user = ubuntu
private_key_file = /var/lib/jenkins/key/mongodb-key.pem
interpreter_python = auto_silent

roles_path = ./roles

timeout = 60
forks = 3

[ssh_connection]
pipelining = True
retries = 3
